import { VStack, Heading, Center } from "native-base";

export function MapView() {
  return (
    <VStack flex={1}>
      <Center>
        <Heading my={24}>MapView</Heading>
      </Center>
    </VStack>
  );
}
