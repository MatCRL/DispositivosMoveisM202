import { VStack, Heading, Center } from "native-base";

export function QRCode() {
  return (
    <VStack flex={1}>
      <Center>
        <Heading my={24}>QRCode</Heading>
      </Center>
    </VStack>
  );
}
